export const MAX_WORLD_RADIUS = 90;
export const MAX_ROCKS = Math.floor(Math.pow(MAX_WORLD_RADIUS, 1.25));
export const MAX_PILLARS = 100;
export const MAX_SPIKES = 800;
export const MAX_HEIGHT = 10;
